// Shared filter types used across table components
 
// Comparison operators for age filter
export type AgeOperator = "=" | ">=" | ">" | "<=" | "<" | "";

// Tab types for switching between Users and Posts
export type Tab = "Users" | "Posts";

